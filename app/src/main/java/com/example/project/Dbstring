
        String url="jdbc:mysql://" + _server+"/"+_DB ;
        Connection con = null;
        String driver = "com.mysql.cj.jdbc.Driver";
        try {
            Class.forName(driver);
        }
        catch (ClassNotFoundException e) {
            e.printStackTrace();
        }
        boolean connected = false;
        int errors = 0;
        String connString = String.format("jdbc:mysql://192.168.1.113:1432/?user=USERaPP?password=tony147...");
        while(!connected) {
            try{
           //     con = DriverManager.getConnection(connString);
                connected = true;

                //Display form allowing user to authenticate login

            } catch(SQLException e) {

                if (errors == 0) {
                    //Display Message "Attempting to connect to database"
                }
                ++errors;

                try {
                    Thread.sleep(1000); // wait a little before the next attempts
                } catch (InterruptedException ex) {
                    ex.printStackTrace();
                }
            }
        }
